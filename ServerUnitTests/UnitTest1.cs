using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Server;
using System.Data;
using System.Linq;
using System.Net.Sockets;
namespace ServerUnitTests
{
   [TestClass]
   public class UnitTest1
   {
      [TestMethod]
      public void Test()
      {
         //Server.
         //TcpListener fakeServer = new TcpListener(System.Net.IPAddress.Loopback, 12345);
         //TcpClient fakeClient = fakeServer.AcceptTcpClient();
         //Server.ClientConnection a = new Server.ClientConnection(fakeClient,);


      }
   }
}
